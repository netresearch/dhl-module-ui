define([
        "Magento_Ui/js/form/form"
    ], function (Component) {
        return Component.extend({
            /** @TODO: handle form flow, provide save callback etc. */
            initialize: function () {
                this._super();
            }
        });
    }
);