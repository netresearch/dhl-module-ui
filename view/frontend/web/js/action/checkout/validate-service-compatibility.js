define([
    'underscore',
    'uiRegistry',
    'Dhl_Ui/js/model/checkout/service/service-selections',
], function (_, registry, serviceSelection) {
    'use strict';

    /**
     * Check for unavailable service combinations and trigger validation events for Service Block.
     *
     * @return {boolean}
     */
    return function () {
        return true
    };

});
